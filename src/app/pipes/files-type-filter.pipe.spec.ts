import { FilesTypeFilterPipe } from './files-type-filter.pipe';

describe('FilesTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FilesTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
