import { TotalRecordsFilterPipe } from './total-records-filter.pipe';

describe('TotalRecordsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalRecordsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
